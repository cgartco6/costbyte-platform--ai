<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CostByte | AI Venture Studio – Launch Your Startup</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script src="https://js.stripe.com/v3/"></script>
    <script src="https://www.paypal.com/sdk/js?client-id={{PAYPAL_CLIENT_ID}}&currency=ZAR"></script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap');
        body { font-family: 'Inter', sans-serif; }
        .color-preview { width: 40px; height: 40px; border-radius: 8px; cursor: pointer; border: 2px solid transparent; transition: all 0.2s; }
        .color-preview.selected { border-color: black; transform: scale(1.05); }
        .step { transition: all 0.3s ease; }
    </style>
</head>
<body class="bg-white text-slate-900">

    <nav class="sticky top-0 z-50 bg-white/80 backdrop-blur px-6 py-4 border-b shadow-sm">
        <div class="max-w-7xl mx-auto flex justify-between items-center">
            <div class="font-extrabold text-2xl flex items-center gap-2">
                <i data-lucide="award" class="w-6 h-6 text-indigo-600"></i> CostByte
            </div>
            <div class="flex items-center gap-4">
                <button onclick="showAuthModal('login')" class="text-sm font-semibold">Login</button>
                <button onclick="showAuthModal('register')" class="bg-slate-900 text-white px-5 py-2 rounded-full text-sm font-bold">Start Venture</button>
            </div>
        </div>
    </nav>

    <main class="max-w-7xl mx-auto px-6 py-12">
        <!-- Hero Section -->
        <div class="text-center mb-16">
            <h1 class="text-5xl md:text-6xl font-extrabold tracking-tighter mb-4">From Idea to <span class="text-indigo-600">Launched Startup</span></h1>
            <p class="text-xl text-slate-600 max-w-2xl mx-auto">Real business plans, logos, websites, legal docs – all generated by AI. Pay once, download everything.</p>
        </div>

        <!-- Step 1: Describe Your Venture -->
        <div class="bg-slate-50 rounded-3xl p-8 mb-12">
            <h2 class="text-2xl font-bold mb-4">1. Describe your business idea</h2>
            <textarea id="vision" rows="4" class="w-full p-4 bg-white rounded-xl border border-slate-200" placeholder="Example: A subscription box for organic pet treats targeting Cape Town pet owners..."></textarea>
        </div>

        <!-- Step 2: Choose Your Branding -->
        <div class="bg-slate-50 rounded-3xl p-8 mb-12">
            <h2 class="text-2xl font-bold mb-4">2. Customize your brand identity</h2>
            <div class="grid md:grid-cols-2 gap-8">
                <div>
                    <label class="block text-sm font-bold mb-2">Primary Color</label>
                    <input type="color" id="primaryColor" value="#4F46E5" class="w-20 h-12 rounded border">
                    <label class="block text-sm font-bold mt-4 mb-2">Secondary Color</label>
                    <input type="color" id="secondaryColor" value="#F59E0B">
                    <label class="block text-sm font-bold mt-4 mb-2">Font Style</label>
                    <select id="fontFamily" class="p-2 rounded border">
                        <option value="Inter">Inter (Modern)</option>
                        <option value="Roboto">Roboto (Clean)</option>
                        <option value="Playfair Display">Playfair (Elegant)</option>
                        <option value="Montserrat">Montserrat (Bold)</option>
                    </select>
                </div>
                <div>
                    <label class="block text-sm font-bold mb-2">Logo Style</label>
                    <select id="logoStyle" class="p-2 rounded border w-full">
                        <option value="minimal">Minimal / Abstract</option>
                        <option value="bold">Bold / Geometric</option>
                        <option value="elegant">Elegant / Serif</option>
                        <option value="playful">Playful / Colorful</option>
                    </select>
                    <div class="mt-4 p-4 bg-white rounded-xl border" id="colorPreview">
                        <p class="text-sm">Preview:</p>
                        <div class="flex gap-2 mt-2">
                            <div class="w-8 h-8 rounded-full" style="background-color: #4F46E5"></div>
                            <div class="w-8 h-8 rounded-full" style="background-color: #F59E0B"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Step 3: Select Package & Payment -->
        <div class="bg-slate-50 rounded-3xl p-8 mb-12">
            <h2 class="text-2xl font-bold mb-4">3. Choose your package (ZAR)</h2>
            <div class="grid md:grid-cols-3 gap-4 mb-8">
                <div class="bg-white p-6 rounded-2xl border-2 border-transparent hover:border-indigo-300 cursor-pointer" onclick="selectPackage('starter')">
                    <p class="font-bold text-lg">Starter</p>
                    <p class="text-3xl font-black">R999</p>
                    <ul class="text-sm mt-4 space-y-1">
                        <li>✓ Business plan PDF</li>
                        <li>✓ Logo + banners</li>
                        <li>✓ Landing page HTML</li>
                        <li>✓ POPIA compliance</li>
                    </ul>
                </div>
                <div class="bg-white p-6 rounded-2xl border-2 border-indigo-600 shadow-lg relative" onclick="selectPackage('professional')">
                    <span class="absolute -top-3 left-6 bg-indigo-600 text-white text-xs px-3 py-1 rounded-full">Most Popular</span>
                    <p class="font-bold text-lg">Professional</p>
                    <p class="text-3xl font-black">R1,999</p>
                    <ul class="text-sm mt-4 space-y-1">
                        <li>✓ Everything in Starter</li>
                        <li>✓ Marketing plan + calendar</li>
                        <li>✓ Full website (5 pages)</li>
                        <li>✓ Social media kit</li>
                    </ul>
                </div>
                <div class="bg-white p-6 rounded-2xl border-2 border-transparent hover:border-indigo-300 cursor-pointer" onclick="selectPackage('enterprise')">
                    <p class="font-bold text-lg">Enterprise</p>
                    <p class="text-3xl font-black">R4,999</p>
                    <ul class="text-sm mt-4 space-y-1">
                        <li>✓ Everything in Professional</li>
                        <li>✓ 30-day marketing campaign</li>
                        <li>✓ Auto-posting to social</li>
                        <li>✓ Priority support</li>
                    </ul>
                </div>
            </div>
            <input type="hidden" id="selectedPackage" value="professional">
            
            <h3 class="font-bold mb-3">Select Payment Method</h3>
            <div class="flex flex-wrap gap-3 mb-6">
                <button onclick="selectPayment('stripe')" class="payment-method-btn px-4 py-2 rounded-full border bg-white">💳 Credit Card (Stripe)</button>
                <button onclick="selectPayment('paypal')" class="payment-method-btn px-4 py-2 rounded-full border bg-white">🅿️ PayPal</button>
                <button onclick="selectPayment('payfast')" class="payment-method-btn px-4 py-2 rounded-full border bg-white">🇿🇦 PayFast</button>
                <button onclick="selectPayment('eft')" class="payment-method-btn px-4 py-2 rounded-full border bg-white">🏦 Direct EFT (FNB)</button>
            </div>
            <input type="hidden" id="selectedPayment" value="stripe">
            
            <div id="payment-button-container" class="mt-4"></div>
            <button id="payNowBtn" onclick="processPayment()" class="w-full bg-indigo-600 text-white py-4 rounded-xl font-bold text-lg hover:bg-indigo-700 transition">Pay Now</button>
        </div>
    </main>

    <!-- Auth Modals -->
    <div id="authModal" class="fixed inset-0 bg-black/50 hidden items-center justify-center z-50">
        <div class="bg-white rounded-2xl p-8 max-w-md w-full">
            <h3 id="authTitle" class="text-2xl font-bold mb-4">Login</h3>
            <input type="email" id="authEmail" placeholder="Email" class="w-full p-3 border rounded-lg mb-3">
            <input type="password" id="authPassword" placeholder="Password" class="w-full p-3 border rounded-lg mb-4">
            <button id="authActionBtn" class="w-full bg-indigo-600 text-white py-3 rounded-lg font-bold">Login</button>
            <button onclick="closeAuthModal()" class="w-full mt-2 text-slate-500">Cancel</button>
        </div>
    </div>

    <script>
        let token = localStorage.getItem('token');
        let selectedPackageValue = 'professional';
        let selectedPaymentMethod = 'stripe';
        let packagePrice = { starter: 999, professional: 1999, enterprise: 4999 };

        function selectPackage(pkg) {
            selectedPackageValue = pkg;
            document.getElementById('selectedPackage').value = pkg;
            // Update UI highlight (simplified)
        }

        function selectPayment(method) {
            selectedPaymentMethod = method;
            document.querySelectorAll('.payment-method-btn').forEach(btn => btn.classList.remove('border-indigo-600', 'bg-indigo-50'));
            event.target.classList.add('border-indigo-600', 'bg-indigo-50');
        }

        async function processPayment() {
            if (!token) { showAuthModal('login'); return; }
            const vision = document.getElementById('vision').value;
            if (!vision) { alert('Please describe your business idea'); return; }
            
            const price = packagePrice[selectedPackageValue];
            const projectRes = await fetch('/api/projects', {
                method: 'POST',
                headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                body: JSON.stringify({ description: vision, package: selectedPackageValue, price, branding: {
                    primary: document.getElementById('primaryColor').value,
                    secondary: document.getElementById('secondaryColor').value,
                    font: document.getElementById('fontFamily').value,
                    logoStyle: document.getElementById('logoStyle').value
                } })
            });
            const { projectId } = await projectRes.json();
            
            if (selectedPaymentMethod === 'stripe') {
                const stripe = Stripe('{{STRIPE_PUBLISHABLE_KEY}}');
                const res = await fetch('/api/payment/create-stripe-payment', {
                    method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                    body: JSON.stringify({ amount: price, projectId })
                });
                const { clientSecret } = await res.json();
                const { error } = await stripe.confirmPayment({ clientSecret, confirmParams: { return_url: window.location.origin + '/payment-success.html' } });
                if (error) alert(error.message);
            } else if (selectedPaymentMethod === 'paypal') {
                const res = await fetch('/api/payment/create-paypal-order', {
                    method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                    body: JSON.stringify({ amount: price, projectId })
                });
                const { orderId } = await res.json();
                window.location.href = `/api/payment/execute-paypal?orderId=${orderId}&projectId=${projectId}`;
            } else if (selectedPaymentMethod === 'payfast') {
                const res = await fetch('/api/payment/create-payfast', {
                    method: 'POST', headers: { 'Authorization': `Bearer ${token}`, 'Content-Type': 'application/json' },
                    body: JSON.stringify({ amount: price, projectId, description: vision })
                });
                const { payfastForm } = await res.json();
                document.body.innerHTML = payfastForm;
                document.forms[0].submit();
            } else if (selectedPaymentMethod === 'eft') {
                alert('Direct EFT: Please pay R' + price + ' to FNB Acc: 62899800711 (CostByte Pty Ltd), Ref: ' + projectId + '. We will confirm within 24h.');
            }
        }

        // Auth functions
        function showAuthModal(type) {
            document.getElementById('authTitle').innerText = type === 'login' ? 'Login' : 'Register';
            document.getElementById('authActionBtn').onclick = () => auth(type);
            document.getElementById('authModal').classList.remove('hidden');
            document.getElementById('authModal').classList.add('flex');
        }
        function closeAuthModal() { document.getElementById('authModal').classList.add('hidden'); }
        async function auth(type) {
            const email = document.getElementById('authEmail').value;
            const password = document.getElementById('authPassword').value;
            const res = await fetch(`/api/auth/${type}`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) });
            const data = await res.json();
            if (res.ok) { localStorage.setItem('token', data.token); token = data.token; closeAuthModal(); alert('Success'); }
            else alert(data.error);
        }

        // Live color preview
        document.getElementById('primaryColor').addEventListener('input', function() { document.querySelectorAll('.color-preview')[0].style.backgroundColor = this.value; });
        lucide.createIcons();
    </script>
</body>
</html>
